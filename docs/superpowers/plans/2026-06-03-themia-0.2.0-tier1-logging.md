# Themia 0.2.0 — Tier 1: `Themia.Logging` Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended)
> or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Port zenity-v1 `Zenity.Core.Logging` → `Themia.Logging` (net10.0) — Serilog-backed logging with a
fluent builder, console/file sinks, and options — by relocation + namespace rename + `Zenity → Themia`
identifier rebrand. Verbatim; v1 had **no tests**, so add one DI smoke test.

**Architecture:** Spec `…/2026-06-02-themia-0.2.0-framework-rename-design.md` §3 ("Caching/Logging port
verbatim — namespace rename only; no Zenity project deps"). Framework layer, `net10.0`, under `src/framework/`.
Independent of `Themia.Framework.Core` and `Themia.Caching`.

**Tech Stack:** .NET 10; `Serilog` (4.1.0) + `Serilog.Extensions.Logging` (8.0.0), `Serilog.Sinks.Console`
(6.0.0), `Serilog.Sinks.File` (6.0.0), `Serilog.Settings.Configuration` (8.0.4), `Serilog.Enrichers.Thread`
(4.0.0), `Serilog.Enrichers.Environment` (3.0.1); `Microsoft.Extensions.{Configuration.Abstractions,
DependencyInjection, Logging, Options}` (9.0.0) — **all already pinned in `Directory.Packages.props`**
(Tier-1 dep prep commit). xUnit; `PublicAPI.*` tracking.

---

## Source-of-truth file map

Source: `~/GitHub/Idevs/single-repo/Packages/nuget/zenity/src/Zenity.Core.Logging/`
Destination: `src/framework/Themia.Logging/` — same relative paths (8 files):

```
Abstractions/{ISerilogSinkRegistration,IZenityLoggingBuilder}.cs
Configuration/LoggingOptions.cs
Extensions/ServiceCollectionExtensions.cs
Internal/{ZenityLogContext,ZenityLoggingBuilder}.cs
Sinks/{ConsoleSinkRegistration,FileSinkRegistration}.cs
```

Brand-named files renamed: `Abstractions/IZenityLoggingBuilder.cs → IThemiaLoggingBuilder.cs`,
`Internal/ZenityLogContext.cs → ThemiaLogContext.cs`, `Internal/ZenityLoggingBuilder.cs → ThemiaLoggingBuilder.cs`.

### Transform rules (apply to every ported file)

1. **Namespaces:** `Zenity.Core.Logging` → `Themia.Logging` (sub-namespaces `.Abstractions`, `.Configuration`,
   `.Extensions`, `.Internal`, `.Sinks`).
2. **Cross-reference usings:** `using Zenity.Core.Logging.*` → `using Themia.Logging.*` (none observed in the
   survey, but apply if present).
3. **Identifier rebrand — every `Zenity` identifier → `Themia`:** types `IZenityLoggingBuilder → IThemiaLoggingBuilder`,
   `ZenityLogContext → ThemiaLogContext`, `ZenityLoggingBuilder → ThemiaLoggingBuilder`; entry method
   `AddZenityLogging → AddThemiaLogging` (all overloads). Update every reference.
4. **Drop ImplicitUsings-covered usings**; keep all others (`Serilog*`, `Microsoft.Extensions.*`).
5. **No logic changes.** Verbatim port + transforms only.

---

### Task 1: Project scaffold + solution wiring

**Files:** Create `src/framework/Themia.Logging/Themia.Logging.csproj`, `PublicAPI.{Shipped,Unshipped}.txt`; modify `Themia.sln`.

- [ ] **Step 1: csproj** (net10.0; deps match v1's `Zenity.Core.Logging.csproj`)

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <PackageId>Themia.Logging</PackageId>
    <Description>Themia logging — Serilog-backed logging with a fluent builder, console/file sinks, thread/environment enrichers, and options.</Description>
    <PackageTags>themia;logging;serilog;structured-logging</PackageTags>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Serilog" />
    <PackageReference Include="Serilog.Extensions.Logging" />
    <PackageReference Include="Serilog.Sinks.Console" />
    <PackageReference Include="Serilog.Sinks.File" />
    <PackageReference Include="Serilog.Settings.Configuration" />
    <PackageReference Include="Serilog.Enrichers.Thread" />
    <PackageReference Include="Serilog.Enrichers.Environment" />
    <PackageReference Include="Microsoft.Extensions.Configuration.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" />
    <PackageReference Include="Microsoft.Extensions.Logging" />
    <PackageReference Include="Microsoft.Extensions.Options" />
    <PackageReference Include="Microsoft.CodeAnalysis.PublicApiAnalyzers">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
  </ItemGroup>
  <ItemGroup>
    <AdditionalFiles Include="PublicAPI.Shipped.txt" />
    <AdditionalFiles Include="PublicAPI.Unshipped.txt" />
  </ItemGroup>
</Project>
```

- [ ] **Step 2:** Create both `PublicAPI.*.txt` with `#nullable enable`.
- [ ] **Step 3:** `dotnet sln Themia.sln add src/framework/Themia.Logging/Themia.Logging.csproj` then build clean (empty).
- [ ] **Step 4: Commit** `git commit -m "feat: scaffold Themia.Logging (net10.0) project"`

### Task 2: Port all source (apply transforms)

**Files:** the 8 files in the map → `src/framework/Themia.Logging/...`.

- [ ] **Step 1:** Port each file applying transform rules 1–5, dependency order (Abstractions → Configuration →
  Sinks → Internal context/builder → Extensions).
- [ ] **Step 2: Build clean** — `dotnet build src/framework/Themia.Logging/Themia.Logging.csproj --no-incremental`.
  (Populate `PublicAPI.Unshipped.txt` incrementally to clear RS0016 under TreatWarningsAsErrors, same as the
  Caching/Framework.Core ports; full surface → Shipped in Task 4.)
- [ ] **Step 3:** `grep -rn "Zenity" src/framework/Themia.Logging` → empty.
- [ ] **Step 4: Commit** `git commit -m "feat: port Themia.Logging source (builder, sinks, log context, options)"`

### Task 3: DI smoke test

**Files:** Create `tests/Themia.Logging.Tests/Themia.Logging.Tests.csproj` + `AddThemiaLoggingTests.cs`.

- [ ] **Step 1: test csproj** (net10.0; reference the package + `Microsoft.Extensions.DependencyInjection`).
- [ ] **Step 2: Write the smoke test**

```csharp
using Microsoft.Extensions.DependencyInjection;
using Microsoft.Extensions.Logging;
using Xunit;

namespace Themia.Logging.Tests;

public class AddThemiaLoggingTests
{
    [Fact]
    public void AddThemiaLogging_RegistersLoggerFactory()
    {
        var services = new ServiceCollection();
        services.AddThemiaLogging();
        using var sp = services.BuildServiceProvider();
        var factory = sp.GetService<ILoggerFactory>();
        Assert.NotNull(factory);
        // A logger can be created without throwing.
        var logger = factory!.CreateLogger("smoke");
        Assert.NotNull(logger);
    }
}
```

> If `AddThemiaLogging()` requires configuration or a builder callback, adapt the arrange to the actual entry
> point (read the ported `ServiceCollectionExtensions`), keeping the assertion: logging is registered and a
> logger can be created.

- [ ] **Step 3:** Add to sln; `dotnet test tests/Themia.Logging.Tests/Themia.Logging.Tests.csproj` → PASS.
- [ ] **Step 4: Commit** `git commit -m "test: Themia.Logging DI registration smoke test"`

### Task 4: PublicAPI surface + gate

- [ ] **Step 1:** Move the full public surface into `PublicAPI.Shipped.txt`; clean build → 0 warnings.
- [ ] **Step 2:** `dotnet build Themia.sln --no-incremental` + `dotnet test Themia.sln` → entire solution green.
- [ ] **Step 3: Commit** `git commit -m "feat: track Themia.Logging public API surface"`

---

## Self-review notes
- **Spec coverage:** verbatim port, namespace rename, no Zenity deps (§3) ✅; `Zenity → Themia` rebrand ✅;
  net10.0 framework layer ✅; no v1 tests → DI smoke test (proportionate).
- **Placeholder scan:** transforms exact; csproj/test/PublicAPI shown; "code" is relocated v1 source.
- **Known risk:** Serilog sink registrations write to console/file; the smoke test only asserts DI wiring +
  logger creation (no sink output assertions), keeping it deterministic and side-effect-free.
